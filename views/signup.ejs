<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sign Up | Wanderlust</title>

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  <style>
    body {
      background: linear-gradient(135deg, #0d1b2a, #1b263b); /* dark gradient */
      color: white;
    }
    .signup-container {
      max-width: 400px;
      background: #ffffff;
      color: #000000;
      padding: 2rem;
      border-radius: 10px;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
    }
  </style>
</head>
<body class="d-flex justify-content-center align-items-center vh-100">

  <div class="signup-container">
    <h1 class="text-center mb-4">Sign Up on To-Do</h1>
    <form action="/signup" method="post" novalidate class="needs-validation">
        
      <!-- Username -->
      <div class="mb-3">
        <input type="text" id="username" placeholder="Add Username" name="username" 
               class="form-control" required>
        <div class="invalid-feedback">Please provide a username.</div>
      </div>

      <!-- Email -->
      <div class="mb-3">
        <input type="email" id="email" placeholder="Add Email" name="email" 
               class="form-control" required>
        <div class="invalid-feedback">Please provide a valid email.</div>
      </div>

      <!-- Password -->
      <div class="mb-3">
        <input type="password" id="password" placeholder="Add Password" name="password" 
               class="form-control" required>
        <div class="invalid-feedback">Please provide a password.</div>
      </div>

      <!-- Submit -->
      <button class="btn btn-primary w-100" type="submit">Sign Up</button>
    </form>

    <!-- Login link -->
    <div class="text-center mt-3">
      <small>Already have an account? 
        <a href="/login" class="text-decoration-none">Log In</a>
      </small>
    </div>
  </div>

</body>
</html>
